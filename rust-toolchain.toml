[toolchain]
# Bumped to 1.91 because cargo-stylus 0.10.5 requires rustc >= 1.91.0.
# Keep this in sync with the cargo-stylus version pinned in scripts/deploy.sh.
channel = "1.91.0"
targets = ["wasm32-unknown-unknown"]
